<IfModule mod_rewrite.c>
    Options -Indexes
    RewriteEngine On
    RewriteBase /ITE311-BUHISAN/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
